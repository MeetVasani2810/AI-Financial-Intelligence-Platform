{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd backend && .venv\\Scripts\\python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "start": "cd backend && .venv\\Scripts\\python -m uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "@repo/database": "workspace:*"
  }
}